For i - 1 To 10
'mod checks if the number is divisible by 2 and then has a remainder of 0
		IF cells (i,1).value Mod 2 = 0 Then
			Cells(i,2).value = "Even Row"
		Else
			Cells(i,2).value = "Odd Row"
		End If
	Next i
End Sub
